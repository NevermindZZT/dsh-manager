# Copy to config.yaml and edit values. Environment variables override this file.
http_addr: "127.0.0.1:18080"
data_dir: "./data"
# database_path: "./data/dsh-manager.db"
# 配对码无需配置；manager 每次启动自动生成并打印新的注册配对码。
admin_username: "admin"
admin_password: "replace-with-dashboard-password"
admin_token: "replace-with-automation-token"
